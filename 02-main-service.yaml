apiVersion: v1
kind: Service
metadata:
 name: nginx
 labels:
  project: roboshop
  tier: frontend
  component: frontend
spec:
 type: LoadBalancer
 selector:
  project: roboshop
  tier: frontend
  component: frontend
 ports:
  - protocol: TCP
    port: 80
    targetPort: 80